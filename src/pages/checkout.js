import Header from "../components/Header";
import styles from '../styles/checkoutPage.sass'

function Checkout() {
    return (
        <div>
            <Header />
            <main className={styles.mainContainer}>
                Header hu main
            </main>
        </div>
    )
}

export default Checkout
